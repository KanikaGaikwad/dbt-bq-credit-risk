version: 2

models:
  - name: stg_loan_risk
    description: "Cleans and standardizes raw loan data from BigQuery."
    columns:
      - name: loan_id
        tests:
          - unique
          - not_null
